import Link from 'next/link';
import {Badge,Empty} from './client';
import {Icon} from './icon';
import {formatDate,type Ticket} from '@/lib/shared';
export function TicketTable({tickets}:{tickets:Ticket[]}){if(!tickets.length)return <Empty title="Belum ada laporan di sini" text="Buat laporan baru atau sesuaikan filter pencarian."/>;return <div className="table-scroll"><table className="ticket-table"><thead><tr><th>Laporan</th><th>Kategori</th><th>Status</th><th>Dibuat</th><th><span className="sr-only">Buka</span></th></tr></thead><tbody>{tickets.map(t=><tr key={t.id}><td><Link className="ticket-title-link" href={`/dashboard/laporan/${t.id}`}><span className="ticket-row-icon"><Icon name={t.sensitive?'shield':'message'} size={18}/></span><span><strong>{t.title}</strong><small className="mono">{t.number}{t.priority==='urgent'&&<em>MENDESAK</em>}</small></span></Link></td><td><span className="table-category">{t.category_name}</span></td><td><Badge status={t.status}/></td><td className="date-cell">{formatDate(t.created_at,true)}</td><td><Link href={`/dashboard/laporan/${t.id}`} className="table-open" aria-label={`Buka ${t.number}`}><Icon name="upRight" size={17}/></Link></td></tr>)}</tbody></table></div>;}
